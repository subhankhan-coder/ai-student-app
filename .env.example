# Server-side only. Read by server/index.js via process.env — never prefix
# this with VITE_, or Vite will bundle it into the public client JS.
OPENAI_API_KEY=sk-your-real-key-here

# Optional — defaults to gpt-4o-mini if omitted.
OPENAI_MODEL=gpt-4o-mini
